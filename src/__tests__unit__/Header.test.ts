import { describe, it, expect } from "vitest";
import TestRenderer from "react-test-renderer";
import Header from "../components/Header";

describe("example()", () => {
  it("checks if 5 is 5", () => {
    const num = 5;
    expect(num).toBe(5);
  });
});

describe("Header.tsx", () => {
  it("test the children inside my header component", () => {});
});
